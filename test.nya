platform {
	[simulator] {
		fn [stdio] write_num(value: i32): () {
			unsafe [stdio] {
				return intrinsic:write_num(value);
			}
		}
	}
}

fn [stdio] main(): i32 {
	let mut a = 0;
	a = 5;
	let v = write_num(a);
	return a;
}
