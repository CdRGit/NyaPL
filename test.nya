fn main(): i32 {
	let tuple = (5, main, 6);

	let value: bool = true;
	let not_value: bool = true == false;

	return tuple_adder(tuple);
}

fn tuple_adder(tuple: (i32, fn(): i32, i32)): i32 {
	let (x, _, y) = tuple;
	return x + y;
}
