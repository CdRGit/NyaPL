platform {
	[simulator] {
		fn [stdio] write_num(value: i32): () {
			unsafe [stdio] {
				intrinsic:write_num(value);
			}
			return ();
		}
	}
}

fn [stdio] main(): i32 {
	fn nested(): i32 {
		return 500;
	}
	let tuple = (500, 200, nested());
	let mut a = 0;
	while (a != nested()) {
		a = a + 1;
	}
	return a;
}
