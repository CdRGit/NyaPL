fn main(): i32 {
	let tuple = (5, main, 6);

	if tuple_comparer(tuple) {
		return 5;
	} elif false {
		return -7;
	}

	return 20;
}

fn tuple_comparer(tuple: (i32, fn(): i32, i32)): bool {
	let (x, _, y) = tuple;
	return x == y;
}
