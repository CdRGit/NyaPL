platform {
	[simulator] {
		fn [stdio] write_num(value: i32): () {
			unsafe [stdio] {
				return intrinsic:write_num(value);
			}
		}
	}
}

fn [stdio] main(): i32 {
	let mut a = 0;
	let mut b = 5;
	if false {
		a = 5;
	} // we need PHI
	elif false {
		a = 6 + a;
	} elif false {
		a = 7 / a;
	} else {
		a = a * a;
		b = 6;
	}
	let v = write_num(a * b);
	return a;
}
