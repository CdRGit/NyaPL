platform {
	[simulator] {
		fn [stdio] write_num(value: i32): () {
			unsafe [stdio] {
				return intrinsic:write_num(value);
			}
		}
	}
}

fn [stdio] main(): i32 {
	let mut m = 0;
	let mut a = false;
	let b = true;
	m = 0;
	if (a) {
		let throwaway = 0;
		m = 3;
		if (b) {
			m = 1;
		} else {
			m = 2;
			a = true;
		}
		m = m * 5;
	}
	let (c, d) = (a, m);
	return d;
}
