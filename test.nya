platform {
	[simulator] {
		fn [stdio] write_num(value: i32): () {
			unsafe [stdio] {
				intrinsic:write_num(value);
			}
			return ();
		}
	}
}

fn [stdio] main(): i32 {
	fn nested(): i32 {
		return 500;
	}
	let mut a = 0;
	let t = (a, nested());
	while (a != nested()) {
		write_num(a);
		a = a + 1;
	}
	let func = nested;
	let another_copy = func;
	return another_copy();
}
