fn main(): i32 {
	// declare a variable without specifying type
	let return_val = 0;

	fn returns(): _ {
		return 0 + 0;
	}

	fn past_the_point(): i32 {
		// of no return
	}

	return return_val + returns();
}
