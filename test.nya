fn main(): i32 {
	let (u, v, w, x, y, z) = (0, 1, 2, 3, 4, 5);
	let mut (a, b) = (0, 1);
	let mut i = 0;
	let mut idx = 13;
	while (i != idx) {
		(a, b) = (b, a + b);
		i = i + 1;
	}
	let c = a + b + i + idx + u + v + w + x + y + z;
	return a;
}
