fn main(): i32 {
	let tuple = (5, main, 6);

	fn foobar(tuple: (_, _, _)): () {
		return ();
	}

	let () = foobar(tuple);

	return tuple_adder(tuple);
}

fn tuple_adder(tuple: (i32, fn(): i32, i32)): i32 {
	let (x, _, y) = tuple;
	return x + y;
}
